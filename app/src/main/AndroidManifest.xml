<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vesit.paddy_ver1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/agricare_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/agricare_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlantDiseaseDetectionApp">
        <activity
            android:name=".SnapTipsPopUp"
            android:exported="true"
            android:theme="@style/Theme.PlantDiseaseDetectionApp.Popup"/>
        <activity
            android:name=".BlurryImageActivity"
            android:exported="true" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity2" />
        <activity android:name=".SelectLanguageActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PhoneLoginActivity" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".AboutActivity" />
    </application>

</manifest>